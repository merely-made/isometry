[workspace]
resolver = "2"
members = [
    "crates/isometry-core",
    "crates/isometry-campaign",
    "crates/isometry-graphshell",
    "crates/isometry-views",
    "crates/isometry-genet",
    "crates/isometry-net",
    "crates/isometry-system",
    "crates/isometry-voxel",
]
exclude = ["crates/isometry-runtime"]

[workspace.package]
edition = "2021"
license = "MIT OR Apache-2.0"
authors = ["Mark <sgt.mark3@gmail.com>"]
repository = "https://github.com/merely-made/isometry"
rust-version = "1.80"

[workspace.dependencies]
# Internal crates
isometry-core = { path = "crates/isometry-core", version = "0.1.0" }
isometry-campaign = { path = "crates/isometry-campaign", version = "0.1.0" }
isometry-voxel = { path = "crates/isometry-voxel", version = "0.1.0" }
isometry-views = { path = "crates/isometry-views", version = "0.1.0" }
isometry-net = { path = "crates/isometry-net", version = "0.1.0" }
isometry-system = { path = "crates/isometry-system", version = "0.1.0" }
# Private host-side choice receipts. The committed source stays on Git; a local
# Cleromancy checkout is supplied by the machine-local source patch during
# paired development.
cleromancy = { git = "https://github.com/merely-made/cleromancy.git", branch = "main", default-features = false }

# Persistence
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
postcard = { version = "1", features = ["alloc"], default-features = false }
# Portable slots/blobs over a host-chosen backend. Isometry uses its redb
# backend for private desktop campaign state; the future web host can swap
# the backend without changing campaign data.
muniment = { git = "https://github.com/merely-made/mere.git", branch = "main" }
# Host-neutral actor runtime for background host/session work. The SerVal
# application owns its state on the winit thread and drains typed updates.
armillary = { git = "https://github.com/merely-made/mere.git", branch = "main" }
personae = { git = "https://github.com/merely-made/mere.git", branch = "main" }
mooting = { git = "https://github.com/merely-made/mere.git", branch = "main" }
# Murm's peer-exchange replication foundation (SyncedSpace/MunimentStore/
# processor). Split out of `mooting` by mere's murm peer-runtime work; mooting
# now keeps only governance policy.
murm-replication = { package = "stickleback", git = "https://github.com/merely-made/mere.git", branch = "main" }
gemot = { git = "https://github.com/merely-made/mere.git", branch = "main" }
# mere renamed the package to `mere-transport` (the lib is still `transport`),
# and `[patch]` keys match the *package* name, so both this dep and the local
# override in .cargo/config.toml must say so.
transport = { package = "mere-transport", git = "https://github.com/merely-made/mere.git", branch = "main" }
murm = { git = "https://github.com/merely-made/mere.git", branch = "main" }
p2panda-core = "0.7.0"
p2panda-net = "0.7.0"
p2panda-store = { version = "0.7.0", default-features = false }
p2panda-sync = "0.7.0"
thiserror = "1"

# Product-free projection score and analytic solvers. Isometry owns the
# campaign-to-score adapter in `isometry-views`; the shared crates carry only
# opaque source refs, measured footprints, and scenes.
sceno = { git = "https://github.com/merely-made/mere.git", branch = "main" }
scenomise = { git = "https://github.com/merely-made/mere.git", branch = "main" }
graphshell-endpoint = { git = "https://github.com/merely-made/mere.git", branch = "main" }
# The projection contract. Was `graphshell-protocol`; mere renamed it to
# `chirograph` on 2026-08-14 because the contract is the family's, not the
# portal's. Same types, same re-exports.
chirograph = { git = "https://github.com/merely-made/mere.git", branch = "main" }
graphshell-stdio = { git = "https://github.com/merely-made/mere.git", branch = "main" }

# UI: the Genet host stack (I1, the Woodshed pattern). Git deps on the
# owned repos so any machine resolves; local checkouts override via the
# gitignored .cargo/config.toml source-specific [patch] entries. Cambium
# supplies the view toolkit while Genet supplies the engine and host crates.
# **Git, not crates.io** (Mark's ruling, 2026-07-26): the family resolves from
# genet.git until there is an actual GitHub release to consume, and crates.io
# releases keep being cut for external consumers rather than for us. Isometry was
# the only repo still taking these from the registry, and it was the one carrying
# the hazard: published cambium/sprigging resolve `paint_list_api` from crates.io
# while this workspace git-deps netrender's, so `sprigging::PaintCmd` was not
# `paint_list_api::PaintCmd`. The gitignored [patch] file hid that here by
# redirecting sprigging to a path; a clean checkout would not have.
#
# The node-label floor the registry pin used to state is now carried by the
# branch itself: `with_node_labels` labels only carry node state from 0.3.2 on,
# and main is past it.
cambium = { git = "https://github.com/merely-made/genet.git", branch = "main" }
cambium-winit = { git = "https://github.com/merely-made/genet.git", branch = "main" }
# Cambium's retained custom-paint leaves (the graph-canvas swatch's painted
# nodes + edges). A host wires them directly, the pelt-desktop pattern. From the
# same git source as cambium, which is what keeps one `PaintCmd` in the graph.
sprigging = { git = "https://github.com/merely-made/genet.git", branch = "main" }
layout-dom-api = { git = "https://github.com/merely-made/genet.git", branch = "main" }
genet-scripted-dom = { git = "https://github.com/merely-made/genet.git", branch = "main" }
genet-layout = { git = "https://github.com/merely-made/genet.git", branch = "main" }
genet-winit-host = { git = "https://github.com/merely-made/genet.git", branch = "main" }
netrender = { git = "https://github.com/merely-made/netrender.git", branch = "main" }
paint_list_api = { git = "https://github.com/merely-made/netrender.git", branch = "main" }
paint_list_render = { git = "https://github.com/merely-made/netrender.git", branch = "main" }
# wgpu for host-side surface/compose types only; backend features come
# from netrender's own per-target manifest split.
wgpu = { version = "30", default-features = false, features = ["std"] }
winit = "0.30"

# Mirror of genet's [patch.crates-io] entries this workspace's genet
# graph needs; patches are per-workspace-root and do NOT inherit through
# a git dep. Copied from the woodshed root (the maintained model).
[patch.crates-io]
# `murm-replication` (from mere) depends on the *published* muniment, while this
# workspace tracks the repo. Two sources means two `muniment::Backend` traits and
# `MunimentStore`'s bounds stop matching, so redirect the registry edge at the
# same git source: exactly one muniment lands. Track the branch, not a pin.
muniment = { git = "https://github.com/merely-made/mere.git", branch = "main" }
layout-dom-api = { git = "https://github.com/merely-made/genet.git", branch = "main", version = "=0.1.0" }
genet-scripted-dom = { git = "https://github.com/merely-made/genet.git", branch = "main", version = "=0.1.0" }
# Only one package in the graph may declare `links = "servo_style_crate"`, so
# stylo_taffy's Stylo and genet-layout's Stylo must be the same package.
#
# This used to be done by patching crates.io `stylo` at the mark-ik fork. That
# stopped working when genet renamed the fork to `genet-stylo` on 2026-07-12
# (genet's T3): `[patch]` cannot rename its target, because the patch source's
# package name must match the registry name it replaces. Genet's answer, which
# this mirrors, is to redirect `stylo_taffy` itself at genet's vendored fork,
# whose own manifest git-deps `genet-stylo` directly. genet-layout resolves the
# same git source, so exactly one Stylo lands. Patching `stylo`/`stylo_atoms`
# here is what *creates* the second copy; do not reintroduce it.
stylo_taffy = { git = "https://github.com/merely-made/genet.git", branch = "main" }
taffy = { git = "https://github.com/merely-made/genet.git", branch = "main" }
ipc-channel = { git = "https://github.com/merely-made/genet.git", branch = "main" }
# `murm-replication` (stickleback, from mere) is written against the **owned
# p2panda fork**, not the published crates. mere's manifest repoints the whole
# family there; this workspace did not, so it resolved stock p2panda from
# crates.io and stickleback failed to compile against it. The break surfaced
# 2026-07-31 when mere's `JoinedSpace` began naming a per-lane protocol id
# (`Builder::protocol_id`, a fork API), but the divergence was latent from the
# moment isometry took a mere git dep without mirroring its p2panda pins.
#
# **The family patches together or not at all.** The fork's `p2panda-net`
# path-deps its siblings, so repointing one alone lands two `p2panda-core`
# copies and the trait bounds stop matching. `p2panda-sync` is included even
# though mere's manifest omits it, because this workspace declares it directly
# (line 57) and would otherwise pull a second copy from the registry.
#
# Track the branch, not a pin, and keep no local paths here: the machine-local
# redirect to a working copy belongs in the gitignored `.cargo/config.toml`.
p2panda-auth = { git = "https://github.com/mark-ik/p2panda.git", branch = "main" }
p2panda-core = { git = "https://github.com/mark-ik/p2panda.git", branch = "main" }
p2panda-discovery = { git = "https://github.com/mark-ik/p2panda.git", branch = "main" }
p2panda-encryption = { git = "https://github.com/mark-ik/p2panda.git", branch = "main" }
p2panda-net = { git = "https://github.com/mark-ik/p2panda.git", branch = "main" }
p2panda-store = { git = "https://github.com/mark-ik/p2panda.git", branch = "main" }
p2panda-sync = { git = "https://github.com/mark-ik/p2panda.git", branch = "main" }
# `p2panda-stream` is deliberately absent, though mere patches it. Nothing in
# this workspace reaches it even under `--all-features`, so the entry only
# produced a permanent "patch was not used" warning. That matters more than it
# sounds: a wall of those warnings is what hid this whole bug for a session.
# The omission is safe because the duplicate-copy hazard only applies to crates
# actually in the graph, and if something later pulls `p2panda-stream` from the
# registry the result is a loud trait-bound mismatch, not a silent second copy.
# Add it back at that point.

# Vello on wgpu 30. Upstream vello 0.10 still requires wgpu 29, so netrender
# (and through it this app's render path) takes vello from the owned fork:
# the v0.10.0 tag plus exactly the API changes in linebender/vello#1754.
# `[patch]` applies only from the top-level manifest and is NOT inherited
# from genet or netrender, so every app workspace restates it. Retires by
# deletion when upstream ships a wgpu-30 vello.
vello = { git = "https://github.com/mark-ik/vello.git", branch = "mark-ik/wgpu-30" }
